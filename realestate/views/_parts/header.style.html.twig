<style type="text/css">
    header {
        /* show header on all pages */
        position: fixed;
        top: 0;
        left: 0;
        /* height must match body::margin-top */
        height: 80px;
        border: 0;
        margin: 0;
        padding: 0;
        width: 100%;
        box-sizing: border-box;
        overflow: hidden;
        font-size: 11px;
        background-color: white;
    }

    header.debug {
        outline: solid 1px blue;
    }

    header table td {
        vertical-align: top;
        padding: 0px;
    }

    header .condominium-info {
        padding-left: 10px;
        text-transform: uppercase;
    }

    header .condominium-info.debug {
        outline: solid 1px red;
    }

    header img.organisation-logo {
        height: 75px;
        /* #memo - we should keep this but it can lead to stretching the image, and object-fit is not supported */
        /*max-height: 100px;*/
        max-width: 250px;
        border: 0;
    }

    header .organisation-info {
        padding-left: 10px;
        text-transform: uppercase;
        text-align: right;
    }

    .main-header {
        padding-top: 30px;
        margin-bottom: 30px;
    }

    .main-header table, .main-header table tr th, .main-header table tr td {
        border: 0 !important;
    }

    .main-header .recipient-info {
        text-align: left;
    }

    .main-header .recipient-info.debug {
        outline: solid 1px green;
    }

    .main-header .document-info {
        text-align: left;
    }

    .main-header .document-info .fiscal_year-ref {
        width: 100%;
        overflow: hidden
    }

    .main-header .document-info .fiscal_year-ref.debug {
        outline: solid 1px turquoise;
    }

    .main-header .document-info .fiscal_year-info {
        width: 100%;
        text-align: left;
        overflow: hidden;
    }

    .main-header .document-info .fiscal_year-info.debug {
        outline: solid 1px yellow;
    }

    .main-header .main-title {
        font-size: 15px;
        font-weight: bold;
        text-align: center;
    }

    main.debug {
        outline: solid 1px brown;
    }

</style>